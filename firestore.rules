rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 게이트는 Cloudflare Worker로 분리됨.
    // 기록 데이터(states)는 모든 사용자 read/write 허용.
    match /classes/{classId}/states/{docId} {
      allow read, write: if true;
    }
  }
}
